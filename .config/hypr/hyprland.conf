# ===========================
# OPTIONS
# ===========================

monitor=,preferred,auto,1

input {
    kb_file=
    kb_layout=
    kb_variant=
    kb_model=
    kb_options=
    kb_rules=

    follow_mouse=1

    touchpad {
        natural_scroll=yes
    }

    sensitivity=0 # -1.0 - 1.0, 0 means no modification.
}

general {
    main_mod=SUPER

    layout=master

    gaps_in=5
    gaps_out=20
    border_size=2
    col.active_border=0x66ee1111
    col.inactive_border=0x66333333

    apply_sens_to_raw=0 # whether to apply the sensitivity to raw input (e.g. used by games where you aim using your mouse)

    damage_tracking=full # leave it on full unless you hate your GPU and want to make it suffer
}

decoration {
    rounding=10
    blur=1
    blur_size=3 # minimum 1
    blur_passes=1 # minimum 1
    blur_new_optimizations=1
}

animations {
    enabled=1
    animation=windows,1,7,default
    animation=border,1,10,default
    animation=fade,1,10,default
    animation=workspaces,1,6,default
}

dwindle {
    pseudotile=0 # enable pseudotiling on dwindle
}

gestures {
    workspace_swipe=no
}

# ===========================
# WINDOW RULES
# ===========================

windowrule=workspace 4,title:^(Telegram)(.*)$

# ===========================
# KEYBINDS
# ===========================

# 1. Windows management

bind=SUPER_ALT,Q,exit,

bind=SUPER,W,killactive,
bind=SUPER,F,fullscreen,0
bind=SUPER_SHIFT,F,tooglefloating,current
bind=SUPER_ALT,F,pin

bind=SUPER,H,movefocus,l
bind=SUPER,L,movefocus,r
bind=SUPER,K,movefocus,u
bind=SUPER,J,movefocus,d

bind=SUPER_SHIFT,H,movewindow,l
bind=SUPER_SHIFT,L,movewindow,r
bind=SUPER_SHIFT,K,movewindow,u
bind=SUPER_SHIFT,J,movewindow,d

bind=SUPER_CTRL,H,resizeactive,-5 0
bind=SUPER_CTRL,L,resizeactive,5 0
bind=SUPER_CTRL,J,resizeactive,0 -5
bind=SUPER_CTRL,K,resizeactive,0 5

bind=SUPER_ALT,H,moveactive,-5 0
bind=SUPER_ALT,L,moveactive,5 0
bind=SUPER_ALT,J,moveactive,0 -5
bind=SUPER_ALT,K,moveactive,0 5


bind=SUPER,1,workspace,1
bind=SUPER,2,workspace,2
bind=SUPER,3,workspace,3
bind=SUPER,4,workspace,4
bind=SUPER,5,workspace,5
bind=SUPER,6,workspace,6
bind=SUPER,7,workspace,7
bind=SUPER,8,workspace,8
bind=SUPER,9,workspace,9
bind=SUPER,0,workspace,10

bind=SUPER_SHIFT,1,movetoworkspace,1
bind=SUPER_SHIFT,2,movetoworkspace,2
bind=SUPER_SHIFT,3,movetoworkspace,3
bind=SUPER_SHIFT,4,movetoworkspace,4
bind=SUPER_SHIFT,5,movetoworkspace,5
bind=SUPER_SHIFT,6,movetoworkspace,6
bind=SUPER_SHIFT,7,movetoworkspace,7
bind=SUPER_SHIFT,8,movetoworkspace,8
bind=SUPER_SHIFT,9,movetoworkspace,9
bind=SUPER_SHIFT,0,movetoworkspace,10

bind=SUPER,mouse_down,workspace,e+1
bind=SUPER,mouse_up,workspace,e-1

# 2. Apps & Utilities

bind=SUPER,P,exec,wofi -i -M fuzzy --show drun
bind=SUPER,RETURN,exec,alacritty
bind=SUPER_SHIFT,RETURN,exec,google-chrome-stable
bind=SUPER_ALT,RETURN,exec,firefox
bind=,Print,exec,flameshot gui

binde=SUPER,B,exec,brightnessctl s 1%+
binde=SUPER_SHIFT,B,exec,brightnessctl s 1%-

binde=SUPER,V,exec,pamixer -i 1
binde=SUPER_SHIFT,V,exec,pamixer -d 1

